{
  "scripts": {
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "deploy": "npm run deploy --prefix backend && npm run deploy --prefix frontend"
  },
  "dependencies": {
    "firebase": "^12.6.0"
  }
}